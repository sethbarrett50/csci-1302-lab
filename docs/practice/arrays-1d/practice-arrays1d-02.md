# Practice Arrays 1D 02 — “Search + Replace”

Given an array of strings:
- find the index of a target string
- replace all occurrences of a string with another string
- print the updated array

## Self-check
- Handles “not found”
- Does not crash on empty array