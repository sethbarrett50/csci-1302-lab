# Lab 08 — Abstract Classes Practice (Ungraded)

This is an **optional, ungraded** practice lab.

## Goal
Practice:
- `abstract` classes and `abstract` methods
- sharing concrete behavior in a base class while forcing derived classes to supply their own piece
- why `Notification` can never be instantiated directly

Practice prompts:
- `docs/practice/abstract-classes/practice-abstract-classes-01.md`

## Run
From repo root:
```bash
make run LAB=src/labs/08-abstract-classes/CS1302.Lab08.AbstractClasses
```
